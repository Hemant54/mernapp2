import React, { Component } from 'react';
import Header from '../Header/Header';

export default class Blogs extends Component {
    render() {
        return (
            <div>
                <Header component="blogs" />
            </div>
        )
    }
}
