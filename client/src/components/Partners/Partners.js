import React, { Component } from 'react';
import Header from '../Header/Header';

export default class Partners extends Component {
    render() {
        return (
            <div>
                <Header component="partners" />
            </div>
        )
    }
}
