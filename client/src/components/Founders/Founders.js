import React, { Component } from 'react';
import Header from '../Header/Header';

export default class Founders extends Component {
    render() {
        return (
            <div>
                <Header component="founders" />
            </div>
        )
    }
}
